#include <iostream>
#include <string>
#include<cmath>
using namespace std;

int main()
{
    int a[12];
    int x;
    int t;
    int A = sizeof(a) / sizeof(a[0])-1;
    for (int i = 0; i <= A; i ++) { 
        if (a[x]>a[x+1]){
            t=
            
        }
       
    }


}
